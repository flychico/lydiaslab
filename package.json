{
  "name": "lydiaslab",
  "private": true,
  "description": "LyDia MLB prediction site — GitHub Actions scripts. No package.json existed before 2026-08-09; this was added solely so scripts/lib/kprops-xlsx.js can write .xlsx files with exceljs. Everything else in scripts/ still uses only Node built-ins.",
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
